// Actions
// Action Creators
// Initial State
const initialState = {
  _id: null,
  marca: null,
  modelo: null,
  ano_fabricacao: null,
  ano_modelo: null,
  combustivel: null,
  cor: null,
  usado: null,
};


// Reducer
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
